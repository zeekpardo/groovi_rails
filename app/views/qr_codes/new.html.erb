<% content_for :title, t("qr_codes.new.title") %>

<div class="container mx-auto p-4">
  <div class="mb-6">
    <%= link_to t("common.back"), qr_codes_path, 
        class: "btn btn-secondary btn-sm mb-4" %>
    <h1><%= t("qr_codes.new.title") %></h1>
  </div>

  <%= render 'form', qr_code: @qr_code %>
</div>